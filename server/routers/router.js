const express = require('express')const router = express.Router()const {register, login, getUser, exit, checkImg, createAd, getAds, getAd, reservation, search} = require('../controllers/controllers')const {registerCheck, loginCheck, checkAd, checkReservation} = require('../middles/validators')router.post('/register', registerCheck, register)router.post('/login', loginCheck, login)router.get('/get-user/:id', getUser)router.get('/exit', exit)router.post('/check-img', checkImg)router.post('/create', checkAd, createAd)router.get('/get-ads', getAds)router.get('/single-ad/:id', getAd)router.post('/reservation',checkReservation, reservation)router.post('/search', search)module.exports = router